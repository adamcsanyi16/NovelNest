function ketElem(first, second, value1, value2, data) {
  return (
    (first && second) ||
    (data.toLowerCase() === `${value1}` && data.toLowerCase() === `${value2}`)
  );
}

function haromElem(first, second, third, value1, value2, value3, data) {
  return (
    (first && second && third) ||
    (data.toLowerCase() === `${value1}` &&
      data.toLowerCase() === `${value2}` &&
      data.toLowerCase() === `${value3}`)
  );
}

function negyElem(
  first,
  second,
  third,
  fourth,
  value1,
  value2,
  value3,
  value4
) {
  return (
    (first && second && third && fourth) ||
    (story.kategoria.toLowerCase() === `${value1}` &&
      story.kategoria.toLowerCase() === `${value2}` &&
      story.kategoria.toLowerCase() === `${value3}` &&
      story.kategoria.toLowerCase() === `${value4}`)
  );
}

function otElem(
  first,
  second,
  third,
  fourth,
  fifth,
  value1,
  value2,
  value3,
  value4,
  value5
) {
  return (
    (first && second && third && fourth && fifth) ||
    (story.kategoria.toLowerCase() === `${value1}` &&
      story.kategoria.toLowerCase() === `${value2}` &&
      story.kategoria.toLowerCase() === `${value3}` &&
      story.kategoria.toLowerCase() === `${value4}` &&
      story.kategoria.toLowerCase() === `${value5}`)
  );
}

function hatElem(
  first,
  second,
  third,
  fourth,
  fifth,
  sixth,
  value1,
  value2,
  value3,
  value4,
  value5,
  value6
) {
  return (
    (first && second && third && fourth && fifth && sixth) ||
    (story.kategoria.toLowerCase() === `${value1}` &&
      story.kategoria.toLowerCase() === `${value2}` &&
      story.kategoria.toLowerCase() === `${value3}` &&
      story.kategoria.toLowerCase() === `${value4}` &&
      story.kategoria.toLowerCase() === `${value5}` &&
      story.kategoria.toLowerCase() === `${value6}`)
  );
}

function hetElem(
  first,
  second,
  third,
  fourth,
  fifth,
  sixth,
  seventh,
  value1,
  value2,
  value3,
  value4,
  value5,
  value6,
  value7
) {
  return (
    (first && second && third && fourth && fifth && sixth && seventh) ||
    (story.kategoria.toLowerCase() === `${value1}` &&
      story.kategoria.toLowerCase() === `${value2}` &&
      story.kategoria.toLowerCase() === `${value3}` &&
      story.kategoria.toLowerCase() === `${value4}` &&
      story.kategoria.toLowerCase() === `${value5}` &&
      story.kategoria.toLowerCase() === `${value6}` &&
      story.kategoria.toLowerCase() === `${value7}`)
  );
}

function nyolcElem(
  first,
  second,
  third,
  fourth,
  fifth,
  sixth,
  seventh,
  eigth,
  value1,
  value2,
  value3,
  value4,
  value5,
  value6,
  value7,
  value8
) {
  return (
    (first &&
      second &&
      third &&
      fourth &&
      fifth &&
      sixth &&
      seventh &&
      eigth) ||
    (story.kategoria.toLowerCase() === `${value1}` &&
      story.kategoria.toLowerCase() === `${value2}` &&
      story.kategoria.toLowerCase() === `${value3}` &&
      story.kategoria.toLowerCase() === `${value4}` &&
      story.kategoria.toLowerCase() === `${value5}` &&
      story.kategoria.toLowerCase() === `${value6}` &&
      story.kategoria.toLowerCase() === `${value7}` &&
      story.kategoria.toLowerCase() === `${value8}`)
  );
}

function kilencElem(
  first,
  second,
  third,
  fourth,
  fifth,
  sixth,
  seventh,
  eigth,
  ninth,
  value1,
  value2,
  value3,
  value4,
  value5,
  value6,
  value7,
  value8,
  value9
) {
  return (
    (first &&
      second &&
      third &&
      fourth &&
      fifth &&
      sixth &&
      seventh &&
      eigth &&
      ninth) ||
    (story.kategoria.toLowerCase() === `${value1}` &&
      story.kategoria.toLowerCase() === `${value2}` &&
      story.kategoria.toLowerCase() === `${value3}` &&
      story.kategoria.toLowerCase() === `${value4}` &&
      story.kategoria.toLowerCase() === `${value5}` &&
      story.kategoria.toLowerCase() === `${value6}` &&
      story.kategoria.toLowerCase() === `${value7}` &&
      story.kategoria.toLowerCase() === `${value8}` &&
      story.kategoria.toLowerCase() === `${value9}`)
  );
}

function tizElem(
  first,
  second,
  third,
  fourth,
  fifth,
  sixth,
  seventh,
  eigth,
  ninth,
  tenth,
  value1,
  value2,
  value3,
  value4,
  value5,
  value6,
  value7,
  value8,
  value9,
  value10
) {
  return (
    (first &&
      second &&
      third &&
      fourth &&
      fifth &&
      sixth &&
      seventh &&
      eigth &&
      ninth &&
      tenth) ||
    (story.kategoria.toLowerCase() === `${value1}` &&
      story.kategoria.toLowerCase() === `${value2}` &&
      story.kategoria.toLowerCase() === `${value3}` &&
      story.kategoria.toLowerCase() === `${value4}` &&
      story.kategoria.toLowerCase() === `${value5}` &&
      story.kategoria.toLowerCase() === `${value6}` &&
      story.kategoria.toLowerCase() === `${value7}` &&
      story.kategoria.toLowerCase() === `${value8}` &&
      story.kategoria.toLowerCase() === `${value9}` &&
      story.kategoria.toLowerCase() === `${value10}`)
  );
}

function tizenegyElem(
  first,
  second,
  third,
  fourth,
  fifth,
  sixth,
  seventh,
  eigth,
  ninth,
  tenth,
  eleventh,
  value1,
  value2,
  value3,
  value4,
  value5,
  value6,
  value7,
  value8,
  value9,
  value10,
  value11
) {
  return (
    (first &&
      second &&
      third &&
      fourth &&
      fifth &&
      sixth &&
      seventh &&
      eigth &&
      ninth &&
      tenth &&
      eleventh) ||
    (story.kategoria.toLowerCase() === `${value1}` &&
      story.kategoria.toLowerCase() === `${value2}` &&
      story.kategoria.toLowerCase() === `${value3}` &&
      story.kategoria.toLowerCase() === `${value4}` &&
      story.kategoria.toLowerCase() === `${value5}` &&
      story.kategoria.toLowerCase() === `${value6}` &&
      story.kategoria.toLowerCase() === `${value7}` &&
      story.kategoria.toLowerCase() === `${value8}` &&
      story.kategoria.toLowerCase() === `${value9}` &&
      story.kategoria.toLowerCase() === `${value10}` &&
      story.kategoria.toLowerCase() === `${value11}`)
  );
}

module.exports = { ketElem };
